<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zjft.usp.uas.right.mapper.SysRoleAppMapper">

    <!-- 角色编号与应用映射列表 -->
    <select id="listRoleAndAppsMap" resultType="java.util.HashMap">
        select b.role_id,GROUP_CONCAT(b.app_id SEPARATOR ',') as app_ids from sys_role a
        join sys_role_app b on a.role_id=b.role_id where a.corp_id=#{corpId}
        group by b.role_id
    </select>
</mapper>
