<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zjft.usp.file.mapper.FileTemporaryMapper">
    <delete id="deleteByTime">
        /*delete from file_temporary where now() >SUBDATE(addtime,interval -1 day);*/
        delete from file_temporary where now() >date_sub(addtime,interval -1 minute);
    </delete>

    <select id="selectFileByTime" resultType="com.zjft.usp.file.model.FileInfo">
        select * from file_info
        where 1=1
        and fileid in (select from file_temporary where now() >date_sub(addtime,interval -1 minute))
    </select>
</mapper>