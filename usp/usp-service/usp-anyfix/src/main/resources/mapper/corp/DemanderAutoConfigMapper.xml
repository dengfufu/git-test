<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zjft.usp.anyfix.corp.manage.mapper.DemanderAutoConfigMapper">

    <select id="listDtoByFilter" resultType="com.zjft.usp.anyfix.corp.manage.dto.DemanderAutoConfigDto">
        select c.*, ds.demander_corp, ds.service_corp
        from demander_auto_config c left join demander_service ds on c.id = ds.id
        where 1=1
        <if test="serviceCorp != null and serviceCorp > 0">
            and ds.service_corp = #{serviceCorp}
        </if>
        <if test="demanderCorp != null and demanderCorp > 0">
            and ds.demander_corp = #{demanderCorp}
        </if>
    </select>

</mapper>
