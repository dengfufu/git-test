<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zjft.usp.anyfix.corp.manage.mapper.DemanderServiceMapper">

    <!-- 根据供应商或客户查询服务商列表 -->
    <select id="listServiceByCorpId" resultType="com.zjft.usp.anyfix.corp.manage.dto.DemanderServiceDto">
        select * from demander_service where demander_corp=#{corpId} or demander_corp in (
        select demander_corp from demander_custom where custom_corp=#{corpId}
        )
    </select>

    <!-- 根据供应商或服务商查询相关企业列表 -->
    <select id="listRelatesCorp" resultType="com.zjft.usp.anyfix.corp.manage.dto.DemanderServiceDto">
        select * from demander_service where demander_corp=#{corpId} or service_corp=#{corpId}
    </select>

</mapper>
