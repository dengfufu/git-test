<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zjft.usp.anyfix.settle.mapper.SettleStaffMapper">
    <select id="querySettleStaffDto" resultType="com.zjft.usp.anyfix.settle.dto.SettleStaffDto">
        select * from
        (select a.*,b.start_date,b.end_date,b.operator,b.operate_time from
        (select * from settle_staff where 1=1
        <if test="settleStaffFilter.userId != null and settleStaffFilter.userId > 0">
            and user_id =#{settleStaffFilter.settleId}
        </if>
        <if test="settleStaffFilter.recordId != null and settleStaffFilter.recordId > 0">
            and record_id =#{settleStaffFilter.recordId}
        </if>
        ) a left join settle_staff_record b on a.record_id=b.record_id where 1=1
        <if test="settleStaffFilter.serviceCorp != null and settleStaffFilter.serviceCorp > 0">
            and b.service_corp =#{settleStaffFilter.serviceCorp}
        </if>
        order by b.operate_time desc) t
    </select>
</mapper>
