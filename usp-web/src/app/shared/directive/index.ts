import {DownFileDirective} from '@shared/directive/down-file.directive';
import {AddressDirective} from '@shared/directive/address.directive';

export const Directives = [
  DownFileDirective,
  AddressDirective
];
